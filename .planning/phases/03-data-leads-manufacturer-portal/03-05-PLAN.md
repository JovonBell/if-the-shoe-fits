---
phase: 03-data-leads-manufacturer-portal
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-04"]
files_modified: []
autonomous: false
requirements:
  - LEAD-01
  - LEAD-02
  - LEAD-03
  - 3DM-04
  - 3DM-05
  - MFR-01
  - MFR-02
  - MFR-03
  - MFR-04
  - MFR-05

must_haves:
  truths:
    - "Complete scan flow ends with lead form submission and STL download"
    - "Jolie can log in to /admin and see customer scans"
    - "Jolie can update order status and download STL files"
    - "Jolie can invite cobbler to the portal"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 flow: lead capture, STL export/download, admin portal login, dashboard, status management, and cobbler invite.

Purpose: Confirm all Phase 3 features work end-to-end before marking the phase complete. This requires a real Supabase project with credentials configured.

Output: User confirmation that all flows work correctly, or a list of issues to address.
</objective>

<execution_context>
@/Users/joshuabellhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuabellhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-leads-manufacturer-portal/03-02-SUMMARY.md
@.planning/phases/03-data-leads-manufacturer-portal/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight checks and build verification</name>
  <files><!-- No files created or modified — verification-only task --></files>
  <action>
Run pre-flight checks to ensure everything compiles and existing tests pass:

1. Run `npx tsc --noEmit` — must exit 0
2. Run `npx vitest run` — all existing tests must pass
3. Run `npm run build` — production build must succeed (catches SSR issues, dynamic import problems, edge runtime compatibility)
4. Verify `.env.local` exists with real Supabase credentials (not example values). If not, inform user they need to:
   - Create a Supabase project at https://supabase.com
   - Copy `.env.local.example` to `.env.local`
   - Fill in NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SERVICE_ROLE_KEY
   - Run the SQL from `supabase/schema.sql` in the Supabase SQL Editor
   - Create a private storage bucket named `stl-files` in Supabase Dashboard -> Storage

If build fails, diagnose and fix issues before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/joshuabellhome/if-the-shoe-fits && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>TypeScript compiles, tests pass, production build succeeds. Environment ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 3 flow</name>
  <files><!-- Checkpoint: no files modified --></files>
  <action>
Present the following verification checklist to the user and wait for confirmation.

What was built in Phase 3:
- Lead capture form inline below scan results (first name + email required, phone + shoe size optional)
- STL export + upload to Supabase Storage on form submit
- STL download button appears after form submission (gated)
- Admin portal at /admin with magic link auth (passwordless)
- Admin dashboard with sortable/filterable customer scan table
- Order status management (new -> in-progress -> completed -> shipped)
- Cobbler invite form (sends magic link via Supabase admin API)

Prerequisites: Supabase project created, `.env.local` configured, schema SQL executed, `stl-files` bucket created. Dev server running (`npm run dev`).

Flow 1 - Lead Capture + STL Download:
1. Open http://localhost:3000 on your phone or browser
2. Complete a foot scan (use the existing wizard flow)
3. On the results page, scroll down past measurements — you should see a lead form with fields: First name*, Email*, Phone (optional), Current shoe size (optional)
4. Try submitting with empty first name — should show validation error
5. Fill in first name + email, submit
6. Should see "You're all set!" message + "Download 3D Foot Model" button
7. Click download — should download a .stl file
8. Check Supabase Dashboard -> Table Editor -> scans — record should exist with measurements and stl_path

Flow 2 - Admin Login:
1. Open http://localhost:3000/admin — should redirect to /admin/login
2. Enter Jolie's email (must be invited first — use Supabase Dashboard -> Auth -> Users -> Invite to create the first admin user)
3. Check email for magic link, click it
4. Should redirect to /admin dashboard

Flow 3 - Admin Dashboard:
1. Dashboard should show the scan from Flow 1
2. Try sorting by clicking column headers (Date, Name, Email, Status)
3. Try filtering by typing in search box
4. Try filtering by status dropdown
5. Change a scan's status via the dropdown — should update immediately (optimistic)
6. Click "Download" on a scan's STL column — should open the file in a new tab

Flow 4 - Cobbler Invite:
1. Scroll to "Invite Team Member" section below the scan table
2. Enter a test email address
3. Click "Send Invite" — should show "Invitation sent successfully!"
4. Check Supabase Dashboard -> Auth -> Users — invited user should appear

Flow 5 - Sign Out:
1. Click "Sign out" in the admin header
2. Should redirect to /admin/login
3. Try accessing /admin directly — should redirect to /admin/login
  </action>
  <verify>User types "approved" or describes specific issues</verify>
  <done>User approves all 5 verification flows or provides issues to fix.</done>
</task>

</tasks>

<verification>
Human confirms all 5 flows work correctly on a real Supabase project.
</verification>

<success_criteria>
- Lead form captures data and stores in Supabase
- STL download works after form submission
- Admin portal accessible via magic link
- Dashboard shows scans with sort/filter/status
- Cobbler invite sends magic link email
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-leads-manufacturer-portal/03-05-SUMMARY.md`
</output>
