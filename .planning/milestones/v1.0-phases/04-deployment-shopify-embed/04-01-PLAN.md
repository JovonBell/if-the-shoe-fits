---
phase: 04-deployment-shopify-embed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - vercel.json
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "Vercel deployment configuration exists with correct headers for camera permission in iframes"
    - "Next.js config adds Permissions-Policy and X-Frame-Options headers allowing iframe embedding"
    - "Production build succeeds without errors"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment configuration"
      contains: "Permissions-Policy"
    - path: "next.config.ts"
      provides: "Next.js headers configuration for iframe camera permission"
      contains: "Permissions-Policy"
  key_links:
    - from: "vercel.json"
      to: "Vercel CDN"
      via: "Vercel reads vercel.json at deploy time"
      pattern: "headers"
    - from: "next.config.ts"
      to: "Response headers"
      via: "Next.js applies headers config to all responses"
      pattern: "Permissions-Policy.*camera"
---

<objective>
Configure Vercel deployment and Next.js response headers so the scanner app can be deployed at a dedicated HTTPS URL and embedded as an iframe with camera permission working in the cross-origin context.

Purpose: The app needs proper headers to allow camera access when embedded as an iframe on Jolie's Shopline storefront. Without `Permissions-Policy: camera=*` and proper frame-ancestors, browsers will block camera access inside the iframe.

Output: vercel.json with deployment config + updated next.config.ts with security headers + verified production build.
</objective>

<execution_context>
@/Users/joshuabellhome/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuabellhome/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-deployment-shopify-embed/04-CONTEXT.md
@next.config.ts
@vercel.json (does not exist yet — will be created)
@src/middleware.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vercel.json and update next.config.ts with deployment headers</name>
  <files>vercel.json, next.config.ts</files>
  <read_first>
    - next.config.ts (current config — standalone output mode)
    - src/middleware.ts (middleware matcher — ensure no conflicts with static assets)
    - .env.local.example (env vars needed in Vercel dashboard)
    - package.json (build script reference)
  </read_first>
  <action>
1. Create `vercel.json` at project root with:
   - Framework preset: nextjs (Vercel auto-detects, but explicit is safer)
   - No buildCommand or outputDirectory overrides (Next.js defaults work)
   - Headers array with a single catch-all route `"source": "/(.*)"` applying:
     - `"Permissions-Policy": "camera=*"` — allows camera in iframes from any origin
     - `"X-Frame-Options": "ALLOWALL"` — deprecated but some older browsers check it
   Note: Do NOT add Content-Security-Policy frame-ancestors here — that would restrict which sites can embed. We WANT any site to embed.

2. Update `next.config.ts` to add the same headers via Next.js config (belt-and-suspenders — Next.js headers apply during `next start` and dev, vercel.json applies on Vercel CDN edge):
   - Add `headers()` async function to nextConfig returning:
     - Source: `"/:path*"` (all routes)
     - Headers: `Permissions-Policy: camera=*`
   - Keep existing `output: 'standalone'` setting
   - Do NOT remove or change any existing config

The `output: 'standalone'` is already set and is correct for Vercel deployment. Do NOT change it to any other value.

Important: Do NOT add `Permissions-Policy: camera=(self)` — that would block camera in cross-origin iframes. Use `camera=*` to allow camera from any embedding origin.
  </action>
  <verify>
    <automated>cd /Users/joshuabellhome/if-the-shoe-fits && cat vercel.json | grep -q "Permissions-Policy" && grep -q "camera=\*" vercel.json && grep -q "Permissions-Policy" next.config.ts && echo "PASS: Headers configured" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - vercel.json exists at project root
    - vercel.json contains `"Permissions-Policy"` header with value `"camera=*"`
    - vercel.json contains `"X-Frame-Options"` header with value `"ALLOWALL"`
    - vercel.json source pattern is `"/(.*)"` (catches all routes)
    - next.config.ts contains `headers` async function
    - next.config.ts `headers()` returns `Permissions-Policy` with `camera=*`
    - next.config.ts still contains `output: 'standalone'`
    - vercel.json is valid JSON (parseable)
  </acceptance_criteria>
  <done>vercel.json and next.config.ts both configure Permissions-Policy: camera=* for all routes. X-Frame-Options: ALLOWALL set in vercel.json. Existing standalone output mode preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Verify production build succeeds with new configuration</name>
  <files></files>
  <read_first>
    - vercel.json (just created — verify it parses)
    - next.config.ts (just updated — verify headers config syntax)
    - package.json (build command reference)
  </read_first>
  <action>
1. Run `npm run build` in the project root to verify the production build succeeds with the updated next.config.ts.
2. If the build fails due to the headers config, fix the syntax. The correct Next.js headers format is:
   ```ts
   async headers() {
     return [
       {
         source: '/:path*',
         headers: [
           { key: 'Permissions-Policy', value: 'camera=*' },
         ],
       },
     ]
   }
   ```
3. Validate vercel.json is valid JSON by parsing it with Node.js: `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8')); console.log('Valid JSON')"`
4. After build succeeds, verify the .next directory was created and the standalone output exists.

Do NOT deploy — this task only verifies the build.
  </action>
  <verify>
    <automated>cd /Users/joshuabellhome/if-the-shoe-fits && npm run build 2>&1 | tail -5 && node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8')); console.log('vercel.json: Valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Build output shows "Generating static pages" or "Route" table (standard Next.js build completion indicators)
    - `.next` directory exists after build
    - `node -e "JSON.parse(...)"` prints "Valid JSON" (no parse errors)
    - No TypeScript errors in next.config.ts
  </acceptance_criteria>
  <done>Production build completes successfully with updated next.config.ts headers configuration. vercel.json is valid JSON. App is ready for Vercel deployment.</done>
</task>

</tasks>

<verification>
1. vercel.json exists and is valid JSON with Permissions-Policy and X-Frame-Options headers
2. next.config.ts has headers() function returning Permissions-Policy: camera=*
3. Production build succeeds (npm run build exits 0)
4. No existing functionality broken (middleware, API routes, pages all compile)
</verification>

<success_criteria>
- vercel.json exists with correct header configuration for iframe camera permission
- next.config.ts updated with headers() without breaking existing config
- Production build succeeds with zero errors
- App is deployment-ready for Vercel
</success_criteria>

<output>
After completion, create `.planning/phases/04-deployment-shopify-embed/04-01-SUMMARY.md`
</output>
